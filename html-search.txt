search for 'item-link'
    next href="
        copy string to "
    next >
        copy string to <
        trim string
    next first rooms
    next >
        copy string to <
    next montly_rent
    next >
        copy string to <
    next size
    next > 
        copy string to <
    next datetime="
        copy string to "
    
if href !== currentHref && dateTime > Date.now
currentHref = href
bot.message(concatenatedString);

// 2017-11-27 17:10:57

// works for YYYY-MM-DD hh:mm:ss
function parse(dateAsString) {
    return new Date(dateAsString.replace(/-/g, '/'))
}